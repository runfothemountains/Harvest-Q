# Harvest Q — Stage 2

This folder contains the Stage-2 demo for Harvest Q: markets directory, interactive map, PWA + AI stubs, and Post Listing flow.

## Quick run (local)
```bash
# from repo root (on stage2 branch)
npx http-server -c-1 . -p 8080
# open http://localhost:8080
